<UserControl x:Class="MKTL.WPF.Views.ActivationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel VerticalAlignment="Center" Margin="20">
        <TextBlock Text="License Required" FontSize="16" Foreground="#CCCCCC" HorizontalAlignment="Center" Margin="0,0,0,15"/>
        
        <TextBox Text="{Binding LicenseKey, UpdateSourceTrigger=PropertyChanged}" 
                 Height="30" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                 Tag="XXXX-XXXX-XXXX" Margin="0,0,0,15"/>
        
        <Button Content="Activate" Command="{Binding ActivateCommand}" 
                Height="32" Background="#555555" Foreground="White" BorderThickness="0" Margin="0,0,0,15"/>
        
        <TextBlock Text="{Binding StatusMessage}" Foreground="{Binding StatusColor}" HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>