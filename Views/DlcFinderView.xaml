<UserControl x:Class="MKTL.WPF.Views.DlcFinderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Bar -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <Button DockPanel.Dock="Right" Content="Search" Command="{Binding SearchCommand}" Width="60"/>
            <TextBox Text="{Binding SearchTerm}" Margin="0,0,5,0" VerticalContentAlignment="Center"/>
        </DockPanel>

        <!-- DLC List -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding DlcList}" AutoGenerateColumns="False" 
                  Background="#222" RowBackground="#2b2b2b" AlternatingRowBackground="#3c3c3c" 
                  Foreground="#DDD" BorderThickness="0" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="80"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2" Text="{Binding Status}" Foreground="#AAA" HorizontalAlignment="Center" Margin="5"/>
    </Grid>
</UserControl>